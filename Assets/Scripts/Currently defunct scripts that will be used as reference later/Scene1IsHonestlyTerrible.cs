using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class Scene1IsHonestlyTerrible : MonoBehaviour // Obsolete code
{
    /// <summary>
    /// This mostly existed as for a while the animation controller of stage 1 was not the same as the rest of the stages.
    /// This was originally my way of trying to change that programmatically.
    /// </summary>
    
    
    // Start is called before the first frame update 
    // public AnimationState[] stateGetter;
    // public AnimatorTransitionBase [] transitionGetter;
    // private GameObject player;
    void Start()
    {
        // player = GameObject.Find("player");

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
